<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Group2_BigProject"
        tools:targetApi="31">

        <activity
            android:name=".WelcomePageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SignUpPageActivity"
            android:exported="false">
        </activity>

        <activity android:name=".SignInPageActivity"
            android:exported="false">

        </activity>

        <activity android:name=".MapPageActivity"
            android:exported="false">

        </activity>

        <activity android:name=".ChatBoxActivity"
            android:exported="false">

        </activity>

        <activity android:name=".ProfilePageActivity"
            android:exported="false">

        </activity>

        <activity android:name=".SocialPageActivity"
            android:exported="false">

        </activity>

        <activity android:name=".ChatSettingsPageActivity"
            android:exported="false">

        </activity>

        <activity android:name=".RoutesPage"
            android:exported="false">

        </activity>

        <activity android:name=".HomePageActivity"
            android:exported="false">

        </activity>

        <activity android:name=".TrackingPageActivity"
            android:exported="false">

        </activity>

        <activity android:name=".ResultPageActivity"
            android:exported="false">

        </activity>
    </application>

</manifest>